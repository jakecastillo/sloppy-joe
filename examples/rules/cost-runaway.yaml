# Extreme cost runaway → throttle the offending tenant for 30m. It auto-reverts
# when spend falls back under threshold (rollback: on_clear) or after the TTL.
on: cost.budget_burn
when: signal.data.spend_1h_usd > 50.0
then:
  - throttle_tenant: {ttl: 30m}
with:
  rollback: on_clear
